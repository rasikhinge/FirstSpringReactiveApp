package com.reactive;

import reactor.core.publisher.Flux;

public class FluxPractice {

    public static void main(String[] args) {
        System.out.println("Flux practice..");
//
//        Flux<Integer> range = Flux.range(4, 5);
////        range.map(i -> i * 2).subscribe(System.out::println);
//        range.flatMap(i -> {
//            return i.toString();
//        });
    }
}
